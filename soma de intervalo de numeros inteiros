#include <iostream>

using namespace std;
int main() {

  int x,y,soma,q;
  cin>>x>>y;
  if(x>=y){
    q=1;
    }
  else{
    q=-1;
  }
  while(x!=y){
    soma=soma+y;
    y=y+q;
  }
  soma=soma+x;
  cout<<"a soma e "<<soma<<endl;
}
